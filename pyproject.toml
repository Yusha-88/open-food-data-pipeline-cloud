[project]
name = "open-food-data-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "pandas-gbq>=0.35.2",
    "tqdm>=4.70.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
